When working in this Java / Spring Boot repo, use MCP tools from **spring-mosaic** before generating or reviewing code.

Suggested flow: `detect_spring_context` → `get_pr_review_brief` → `scan_violations` / `review_architecture`.
